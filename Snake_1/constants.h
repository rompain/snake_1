#pragma once

#define CELLS_X	25
#define CELLS_Y	25

#define SNAKE_CELL_SIZE 15

#define BORDER 10

#define WINDOW_WIDTH (2 * BORDER + SNAKE_CELL_SIZE * (CELLS_X + 2))
#define WINDOW_HEIGHT (BORDER + 60 + SNAKE_CELL_SIZE * (CELLS_Y + 2))
